/**
 * Configuration type/default/schema exports.
 *
 * Kept as a compatibility layer for existing imports.
 */

export * from './config-types';
export { DEFAULT_CONFIG } from './default-config';
export { CONFIG_SCHEMA } from './schema';
